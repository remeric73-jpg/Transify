
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mrico73.geoligne',
  appName: 'GEOligne',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
